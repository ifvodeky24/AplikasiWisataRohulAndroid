<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.aplikasiwisatarohul">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_app"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_app"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.AplikasiWisataRohul"
        tools:targetApi="m">

        <activity android:name=".ui.wisata.WisataBudayaActivity"/>
        <activity android:name=".ui.wisata.WisataReligiActivity" />
        <activity android:name=".ui.wisata.WisataAlamActivity" />
        <activity android:name=".ui.wisata.WisataBuatanActivity" />
        <activity android:name=".ui.wisata.WisataKulinerActivity" />
        <activity android:name=".ui.wisata.WisataSejarahActivity" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.AplikasiWisataRohul.NoActionBar" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.AplikasiWisataRohul.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>